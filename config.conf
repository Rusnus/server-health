CPU_THRESHOLD=80
RAM_THRESHOLD=85
DISK_THRESHOLD=90

SERVICES="ssh ufw fail2ban"

# Telegram
TG_BOT_TOKEN=""
TG_CHAT_ID=""

# Email
EMAIL_TO=""
EMAIL_FROM=""

LOG_FILE="/var/log/server-health.log"
HOSTNAME="$(hostname)"

DAEMON_INTERVAL=300
